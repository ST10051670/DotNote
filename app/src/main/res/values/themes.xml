<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme for AppCompat with dark mode -->
    <style name="Theme.DotNote" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Secondary brand color -->
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background and text -->
        <item name="colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/textPrimary</item>
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>

        <!-- Hint text color -->
        <item name="android:textColorHint">@color/textSecondary</item>

        <!-- Button text color -->
        <item name="android:textColor">@color/textPrimary</item>

        <item name="colorSecondary">@color/darker_gray</item>

    </style>

</resources>
